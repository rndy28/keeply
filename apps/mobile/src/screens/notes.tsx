import { View, Text } from "react-native";
import { withBackground } from "../utils/withBackground";

const Notes = () => {
  return (
    <>
      <Text>notes</Text>
    </>
  );
};

export default withBackground(Notes);
